# Agent Framework — Current Project Facts

**Observed date:** 2026-09-18  
**Repository:** `market-predictions/agent`  
**Status type:** project-local implementation snapshot only  
**Control runtime/status authority:** **no**

> Live GitHub, Modal and canonical Control state override this snapshot. This file never grants lifecycle authority.

## Current implementation state

```text
architecture_version=v0.5-candidate
canonical_mission=AGENT_FRAMEWORK
canonical_mission_revision=2026-09-10-r2
control_managed=true
gap_01_control_state=DONE
gap_05_candidate_present=true
gap_05_user_facing_enabled=false
gap_05_external_review_passed=false
hermes_runtime_pinned=true
freellmapi_runtime_pinned=true
modal_bounded_worker_proven=true
phase1_qualification_runs=20
phase1_human_usable_rate=0.90
interactive_dashboard_runtime=native_hermes
interactive_dashboard_auth=nous_portal_oauth
interactive_dashboard_state=separate_hermes_volume
interactive_dashboard_provider=freellmapi
interactive_direct_provider_credentials=false
trusted_verifier_deployed=false
worker_fanout_deployed=false
framework_database_present=false
framework_queue_present=false
production_project_write_authority=false
```

## Integrated GAP-01 truth

The bounded carrier is integrated and canonical:

```text
PUBLIC_NON_PERSONAL task
  -> pinned Hermes 0.21.1 / exact commit
  -> named FreeLLMAPI provider
  -> protected pinned FreeLLMAPI 0.9.8 service
  -> configured free-provider pool
  -> Hermes live web tool
  -> strict CANDIDATE
```

The fixed 20-run qualification produced 18 strict source-bearing candidates and a 90% human-usable rate. GAP-01 remains generation-only: `CANDIDATE` is not `RESULT_READY` and neither is business `DONE`.

## GAP-05 candidate truth

Owner-approved replenishment A8 authorizes the current GAP-05 candidate only. The candidate adds the first human-facing layer with the smallest native path:

```text
mobile/browser
  -> native Hermes Web Dashboard
  -> native Nous Portal OAuth gate
  -> Hermes managed scope from GitHub
  -> protected FreeLLMAPI
  -> configured free-provider pool
```

Security/capability facts:

- no dashboard fork or second agent runtime;
- one Modal dashboard container;
- interactive state is stored only in a dedicated Hermes Modal Volume;
- Control does not read/write interactive state;
- target-project business truth is not stored in the dashboard;
- model provider and model route are managed/pinned to FreeLLMAPI + `auto`;
- interactive tools are limited to `web`, `memory`, `session_search`;
- approval mode is managed/pinned to `manual`;
- terminal/file/browser/delegation/messaging authority is absent from the interactive toolset;
- direct provider credential names are administrator-managed empty entries, so the native Hermes key writer cannot persist them;
- startup rejects any direct-provider credential already present in the dashboard process environment;
- no target-project production credential enters Hermes.

## Enablement state

The GAP-05 source candidate is **not user-facing enabled**.

Required before enablement:

1. exact-head candidate CI green;
2. owner-provisioned Modal Secret `agent-hermes-dashboard` with valid `HERMES_DASHBOARD_OAUTH_CLIENT_ID=agent:{instance_id}`;
3. fresh external exact-candidate review PASS;
4. explicit deployment/mobile proof through the canonical Modal workflow.

The OAuth client id is external configuration and is intentionally not generated by repository code or committed to Git.

## Persistence behavior

The interactive Hermes home is mounted from dedicated Modal Volume `agent-hermes-dashboard-state`.

- max dashboard containers: one;
- SQLite journal mode: `delete`;
- state commit interval: 30 seconds;
- documented normal cold-restart loss bound: the latest uncommitted interval;
- this state is not a framework DB/queue and carries no Control/project authority.

## Canonical implementation order

Mission revision `2026-09-10-r2` governs:

```text
GAP-01 DONE
  -> GAP-05 current candidate
  -> GAP-02 verifier
  -> GAP-03 parallelism experiment
  -> GAP-04 bounded caller integration
```

Earlier repo text with another sequence is superseded by `docs/ROADMAP.md` in this candidate.

## Cleanup rule

Every consequential change must leave one coherent current truth. Superseded code/config/docs are deleted or rewritten instead of retained as parallel current implementations unless an active requirement explicitly needs them.
